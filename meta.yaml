package:
  name: mpc_hive 
  version: 0.1.0  

source:
  path: .     # Path to the directory containing your built distribution artifacts

requirements:
  build:
    - python=3.10
    - flit
  run:
    - python=3.10

about:
  home: https://github.com/AndrePatri/MPCHive
  license: MIT
  summary: Utilities to create a CPU-based control cluster to be interfaced with GPU-based simulators using shared memory for minimum latency.

extra:
  recipe-maintainers:  
    - AndrePatri
    
    
